{% trans_default_domain 'toolbar' %}

{% spaceless %}
{# Dashboard #}
{{ toolbar_button(path('user_dashboard'), 'dashboard', 'label.user'|trans) }}

{# Display #}
{% if type == 'dashboard' or type == 'modify' or type == 'changePassword' or type == 'delete' %}
    {{ toolbar_button(path('user_display'), 'user') }}
{% endif %}

{# Modify #}
{% if type == 'dashboard' or type == 'display' or type == 'changePassword' or type == 'delete' %}
    {{ toolbar_button(path('user_modify'), 'modify') }}
{% endif %}

{# Cancel #}
{% if type != 'dashboard' and type != 'displayAvailable' and type != 'help' %}
    {{ toolbar_button('cancel', 'cancel') }}
{% endif %}

{# Change password #}
{% if type == 'dashboard' or type == 'display' or type == 'modify' or type == 'delete' %}
    {{ toolbar_button(path('user_change_password'), 'change_password') }}
{% endif %}

{# Help #}
{% if type != 'help' %}
    {{ toolbar_button(path('user_help'), 'help') }}
{% endif %}

{# Signout #}
{{ toolbar_button(path('user_signout'), 'signout') }}

{# Other dashboards #}
{{ toolbar_dashboards('md') }}
{% endspaceless %}